name: Check Internet Connection

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  check-internet:
    runs-on: ubuntu-latest

    steps:
    - run: |
      curl https://www.google.de/
      
